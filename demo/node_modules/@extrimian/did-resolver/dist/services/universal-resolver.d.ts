import { DIDDocument } from "@extrimian/did-core";
import { ModenaResponse } from "../models/modena-response";
export declare class DIDUniversalResolver {
    private config;
    url: string;
    apiKey: {
        fieldName?: string;
        value: string;
        type?: "header" | "queryParam";
    };
    constructor(config: {
        universalResolverURL: string;
        apiKey?: {
            fieldName?: string;
            value: string;
            type?: "header" | "queryParam";
        };
    });
    resolveDID(did: string): Promise<DIDDocument>;
    resolveDIDWithMetadata(did: string): Promise<ModenaResponse>;
}
