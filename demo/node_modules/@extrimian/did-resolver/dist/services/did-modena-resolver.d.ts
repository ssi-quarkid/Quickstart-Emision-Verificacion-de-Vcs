import { DIDDocument } from "@extrimian/did-core";
import { ModenaResponse } from "../models/modena-response";
export declare class DIDModenaResolver {
    private config;
    constructor(config: {
        modenaURL: string;
        apiKey?: {
            fieldName?: string;
            value: string;
            type?: "header" | "queryParam";
        };
    });
    resolveDID(uniqueSuffix: string): Promise<DIDDocument>;
    resolveDIDWithMetadata(did: string): Promise<ModenaResponse>;
}
