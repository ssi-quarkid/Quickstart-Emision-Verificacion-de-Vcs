import { IES256kKeyPair, IES256kSuite, LANG, IJWK } from "@extrimian/kms-core";
export declare class ES256kSuite implements IES256kSuite {
    private wallet;
    constructor();
    verifySignature(originalContent: string, flatSignature: any, publicKey: IJWK): Promise<boolean>;
    private publicKeyToAddress;
    load(secret: IES256kKeyPair): Promise<void>;
    create(params?: {
        lang: LANG;
    }): Promise<IES256kKeyPair>;
    sign(content: string): Promise<string>;
}
