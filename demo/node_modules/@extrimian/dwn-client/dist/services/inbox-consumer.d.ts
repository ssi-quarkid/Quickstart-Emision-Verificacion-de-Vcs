import { Entry, MessageDescriptor } from '../types/message';
export declare class InboxConsumer {
    private readonly inboxURL;
    private readonly did;
    constructor(url: string, did: string);
    getMessages(filters: Omit<MessageDescriptor, 'cid' | 'method'>): Promise<Entry[]>;
    private mapResponse;
}
