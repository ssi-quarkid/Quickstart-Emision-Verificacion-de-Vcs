import { Entry, MessageDescriptor } from '../types/message';
type DescriptorParam = Omit<MessageDescriptor, 'dateCreated' | 'cid'> & {
    dateCreated: Date;
};
export type SendMessageParams = {
    targetDID: string;
    targetInboxURL: string;
    message: {
        data: any;
        descriptor: DescriptorParam;
    };
};
export declare class InboxSender {
    sendMessage(params: SendMessageParams): Promise<Entry[]>;
}
export {};
