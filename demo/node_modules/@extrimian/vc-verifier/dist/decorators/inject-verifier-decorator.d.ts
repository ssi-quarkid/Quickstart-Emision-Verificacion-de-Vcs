import { VCVerifier } from "../algorithm-verifications/vc-verifier";
declare const verifiers: Map<string, new (...args: never[]) => VCVerifier>;
declare const InjectVerifier: (proofType: string) => (target: new (...args: never[]) => VCVerifier) => void;
export { InjectVerifier, verifiers };
