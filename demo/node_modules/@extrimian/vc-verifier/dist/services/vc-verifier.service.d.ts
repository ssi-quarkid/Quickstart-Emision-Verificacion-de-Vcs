import { DIDDocument, Purpose } from "@extrimian/did-core";
import { VerifiableCredential } from "@extrimian/vc-core";
import { VCSuiteError } from "../errors/error-code";
export declare class VCVerifierService {
    private params;
    constructor(params: {
        didDocumentResolver: (did: string) => Promise<DIDDocument>;
    });
    verify(vc: VerifiableCredential | string, purpose: Purpose): Promise<{
        result: boolean;
        error?: VCSuiteError;
    }>;
}
