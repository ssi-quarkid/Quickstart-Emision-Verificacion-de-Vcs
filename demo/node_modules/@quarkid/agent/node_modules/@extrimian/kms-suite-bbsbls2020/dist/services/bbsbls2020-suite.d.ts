import { DIDDocument, VerificationMethod, VerificationMethodPublicKey58 } from "@extrimian/did-core";
import { IVCJsonLDKeyPair } from "@extrimian/kms-core";
import { JsonLDSuite } from "@extrimian/kms-suite-jsonld";
import { VerifiableCredential } from "@extrimian/vc-core";
export declare class BbsBls2020Suite extends JsonLDSuite {
    create(): Promise<IVCJsonLDKeyPair>;
    deriveVC(signedDocument: any, deriveProofFrame: string, didDocumentResolver: (did: string) => Promise<DIDDocument>): Promise<VerifiableCredential>;
    vmConvertions(vm: VerificationMethod): VerificationMethodPublicKey58;
    protected getSuite(params?: {
        verificationMethodId: string;
        controllerDid: string;
    }): Promise<any>;
}
